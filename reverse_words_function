## Reference https://www.geeksforgeeks.org/reverse-words-in-a-given-string/

def reverse_words(string, x):
    # Split words in the given string and reverse the order
    reversed_words = string.split()[::-1]
    x = 123 + x
    # Join the reversed words into a single string
    return " ".join(reversed_words), x

# Input string
string = " geeks for all"
x = 6
reversed_string, updated_x = reverse_words(string, x)

# Print both the reversed string and the updated x value
print(reversed_string)
print(updated_x)
